<% if @items.empty? && @view_my_items %>
  You have no items for sale at the moment
<% elsif @items.empty? %>
  Unable to find the item you're looking for
<% else %>
  <% @items.each do |item|%>
    <%= link_to item.name, item_path(item) %>
    <% if !current_user || not_seller_item(item)%>
      <%= link_to 'Add to cart', :controller => 'items', :action => 'add_to_cart', :id => item.id %><br>
    <% end %>
  <% end %>
<% end %>