<% if @items.empty? %>
  Unable to find the item you're looking for
<% else %>
  <% @items.each do |item|%>
  <%= link_to item.name, item_path(item) %> 
  <%= link_to 'Add to cart', :controller => 'items', :action => 'add_to_cart', :id => item %><br>
  <% end %>
<% end %>